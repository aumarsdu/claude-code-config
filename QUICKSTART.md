# 🚀 快速开始指南

> 5分钟掌握配置管理的核心操作

## 📱 日常使用三步走

### 1️⃣ 查看状态

```bash
~/config-manager.sh status
```

或使用快捷命令：

```bash
cd ~ && git status
```

### 2️⃣ 备份配置

当你修改了配置文件后：

```bash
~/config-manager.sh backup
```

这会自动：
- ✅ 添加变更的文件
- ✅ 创建提交
- ✅ 推送到 GitHub

### 3️⃣ 同步配置

在其他设备上获取最新配置：

```bash
~/config-manager.sh sync
```

## 🔧 管理脚本速查

```bash
# 📊 查看状态
~/config-manager.sh status

# 👀 查看变更详情
~/config-manager.sh diff

# ⬆️ 备份到 GitHub
~/config-manager.sh backup

# ⬇️ 从 GitHub 同步
~/config-manager.sh sync

# 📋 列出所有配置文件
~/config-manager.sh list

# ✅ 检查配置完整性
~/config-manager.sh check

# 💾 在新设备上安装
~/config-manager.sh install

# ❓ 查看帮助
~/config-manager.sh help
```

## ⚡ 设置别名（推荐）

添加到 `~/.zshrc` 或 `~/.bashrc`：

```bash
# 配置管理别名
alias cs='~/config-manager.sh status'
alias cb='~/config-manager.sh backup'
alias csy='~/config-manager.sh sync'
alias cd='~/config-manager.sh diff'
alias cl='~/config-manager.sh list'
```

保存后运行：

```bash
source ~/.zshrc  # 如果使用 zsh
# 或
source ~/.bashrc  # 如果使用 bash
```

现在你可以使用简短命令：

```bash
cs      # 查看状态
cb      # 备份配置
csy     # 同步配置
cd      # 查看变更
cl      # 列出文件
```

## 🔄 典型工作流

### 场景 1：修改了配置，想备份

```bash
# 1. 查看改了什么
cs

# 2. 查看详细变更
cd

# 3. 备份到 GitHub
cb
```

### 场景 2：在新电脑上工作

```bash
# 1. 首次克隆
cd ~
git clone git@github.com:aumarsdu/claude-code-config.git temp
cp -rn temp/. .
rm -rf temp

# 2. 或使用脚本
~/config-manager.sh install
```

### 场景 3：定期同步

```bash
# 早上开始工作前
csy

# 晚上工作结束后
cb
```

## 🛡️ 安全检查清单

在每次备份前，确认：

- [ ] 没有 API 密钥
- [ ] 没有密码或 token
- [ ] 没有私有服务器地址
- [ ] 没有个人身份信息

**提示**：使用 `cd`（diff）命令查看即将提交的内容

## 🆘 常见问题

### Q: 如何撤销还未提交的变更？

```bash
git checkout -- .claude/settings.json
```

### Q: 如何查看提交历史？

```bash
cd ~ && git log --oneline -10
```

### Q: 推送失败怎么办？

```bash
# 检查网络连接
ssh -T git@github.com

# 重新推送
cd ~ && git push origin main
```

### Q: 如何添加新的配置文件？

1. 编辑 `.gitignore`，确保文件没有被排除
2. 使用 `git add` 添加文件
3. 运行 `cb` 备份

### Q: 忘记推送了怎么办？

```bash
# 检查是否有未推送的提交
cd ~ && git log origin/main..HEAD

# 如果有未推送的提交
git push origin main
```

## 📚 进阶使用

查看完整文档：

```bash
cat ~/README.md
```

或访问 GitHub：

https://github.com/aumarsdu/claude-code-config

## 💡 最佳实践

1. **定期备份**：每次修改配置后立即备份
2. **查看变更**：提交前用 `diff` 检查内容
3. **描述清晰**：使用有意义的提交信息
4. **保持同步**：在多台设备间及时同步
5. **安全第一**：永远不要提交敏感信息

---

**需要帮助？** 查看 [README.md](./README.md) 或运行 `~/config-manager.sh help`
